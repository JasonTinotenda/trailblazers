/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./trailblazers/**/*.{html,css,js}"], // Scans all files with html, css, and js extensions in the trailblazers folder and its subfolders
  theme: {
    extend: {}, // You can extend the default Tailwind theme here
  },
  plugins: [], // Add plugins here if needed
};
